
# GERSKA=1 znaci negativni
# greska=2 znaci da nema vise nem
# REZULTAT SE UPISUJE U C
# 
def zbir(a, b):
    kod_gerske = pripremi_operande(a, b)
    if a <= 0 or b <= 0:
        return 1
    
    return 0 # 

# ovde ide rezultat
c = 0
kod_greske  = zbir(2, 2)
if kod_greske == 1:
    # gresak neka
if kod_greske == 2
   neka 